#pragma once

#include <filesystem>

inline const std::filesystem::path PROJECT_ROOT = AGNES_PROJECT_SOURCE_DIR;
inline const std::filesystem::path IMAGE_PATH = PROJECT_ROOT / "images";
inline const std::filesystem::path ASSETS_PATH = PROJECT_ROOT / "Assets";
inline const std::filesystem::path SHADERS_PATH = PROJECT_ROOT / "shaders";